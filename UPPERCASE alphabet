A PROGRAM TO FIND ONLY UPPERCASE ALPHABETS
#include <stdio.h>

int main() {
    char a;
    int res;
    printf("Enter a CHARACTER: ");
    scanf(" %c", &a);
    res = (a >= 'A' && a <= 'Z') ;
    printf("The %c is an uppercase ALPHABET: %d\n", a, res);
    return 0;
}
